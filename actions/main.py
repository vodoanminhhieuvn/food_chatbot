from utils.global_configuration import GlobalConfiguration

# ? This file will execute first time launch config, etc.
# ! Only put first time launch config here

GlobalConfiguration.set_config()

print("Activate config")
